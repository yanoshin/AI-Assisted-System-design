# AI System Design / UML

AIシステムの設計ドキュメント集。設計書とUML図をMarkdownで管理します。

## 方針

- ドキュメントはMarkdown。UML図はテキストベース（Mermaid または PlantUML）で記述し、gitで差分を追える形にする。
- 図は画像バイナリではなくソーステキストで管理し、必要に応じてローカルでレンダリングする。

## 構成

内容が増えてきたら `docs/` 配下を関心ごとに整理する想定です（例: `docs/architecture/`, `docs/uml/`, `docs/decisions/`）。
